services:
  postgres:
    image: postgres:16-alpine
    container_name: trixi-postgres
    environment:
      POSTGRES_DB: trixi_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"